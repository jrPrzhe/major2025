# 🔄 Восстановление базы данных из бэкапа

## Локальное восстановление (уже выполнено)

База данных восстановлена из бэкапа `database-backup-1765446058434.db_` в локальный файл `database.db`.

## Восстановление на сервере (Railway/Render)

### Вариант 1: Через Railway CLI (если установлен)

```bash
# 1. Установите Railway CLI (если еще не установлен)
npm i -g @railway/cli

# 2. Войдите в Railway
railway login

# 3. Подключитесь к проекту
railway link

# 4. Загрузите базу данных
railway run bash
# Затем в консоли:
# cp /path/to/database-backup-1765446058434.db_ /path/to/database.db
```

### Вариант 2: Через Railway Dashboard

1. Откройте Railway Dashboard
2. Перейдите в ваш проект
3. Откройте раздел "Variables" или "Settings"
4. Найдите путь к базе данных (обычно в переменной `DATABASE_PATH`)
5. Используйте Railway CLI для загрузки файла:
   ```bash
   railway run bash
   # Затем загрузите файл через веб-интерфейс или используйте wget/curl
   ```

### Вариант 3: Через админ-панель (если есть доступ к файловой системе)

1. Откройте админ-панель на сервере
2. Если есть возможность загрузить файл, загрузите `database-backup-1765446058434.db_`
3. Переименуйте его в `database.db` и замените существующий файл

### Вариант 4: Ручное восстановление через SSH/Console

Если у вас есть доступ к консоли сервера:

```bash
# 1. Остановите сервер (если нужно)
# 2. Загрузите бэкап на сервер (через SCP, SFTP или веб-интерфейс)
# 3. Замените базу данных
cp database-backup-1765446058434.db_ database.db

# 4. Перезапустите сервер
```

## ⚠️ Важно

- **Остановите сервер** перед заменой базы данных
- **Сделайте новый бэкап** текущей базы перед восстановлением (на всякий случай)
- После восстановления **перезапустите сервер**
- Проверьте, что данные восстановились корректно

## ✅ Проверка после восстановления

1. Откройте приложение
2. Проверьте, что очки игроков восстановлены
3. Проверьте, что пики игроков на месте
4. Проверьте результаты стадий (если были сохранены)

