# 💾 Инструкция по резервному копированию базы данных

## ⚠️ ВАЖНО!

**База данных НЕ сохраняется автоматически в GitHub!** Файл `database.db` находится в `.gitignore` и не загружается в репозиторий.

Если сервер упадет и вы развернете репозиторий заново, **все данные будут потеряны**, если у вас нет резервной копии!

## 📥 Как сделать резервную копию

### Способ 1: Через админ-панель (Рекомендуется)

1. Откройте админ-панель: `https://ваш-домен.com/admin.html`
2. Введите пароль: `admin123`
3. Прокрутите вниз до раздела "💾 Резервное копирование базы данных"
4. Нажмите кнопку "💾 Скачать резервную копию"
5. Сохраните файл `database-backup-XXXXX.db` в безопасное место (Google Drive, Dropbox, локальный компьютер)

### Способ 2: Через API напрямую

```bash
curl -O https://ваш-домен.com/api/admin/backup
```

Или откройте в браузере:
```
https://ваш-домен.com/api/admin/backup
```

## 📤 Как восстановить базу данных

### На Render.com / Railway.app / Fly.io:

1. **Скачайте резервную копию** (если еще не скачали)
2. **Остановите сервер** (через панель управления платформы)
3. **Подключитесь по SSH** (если доступен) или используйте веб-консоль
4. **Замените файл базы данных**:
   ```bash
   # Загрузите файл database.db на сервер
   # Замените существующий файл database.db
   ```
5. **Перезапустите сервер**

### На VPS (через SSH):

```bash
# 1. Остановите сервер
pm2 stop server.js
# или
systemctl stop your-app

# 2. Загрузите резервную копию на сервер (через SCP или SFTP)
scp database-backup-XXXXX.db user@server:/path/to/app/database.db

# 3. Или скопируйте локально, если уже на сервере
cp database-backup-XXXXX.db database.db

# 4. Перезапустите сервер
pm2 start server.js
# или
systemctl start your-app
```

## 🔄 Рекомендации по резервному копированию

### Регулярность:
- **Ежедневно** - если активно используется
- **После важных изменений** - после сохранения результатов стадий, победителя
- **Перед обновлением** - перед любыми изменениями на сервере

### Хранение:
- Сохраняйте несколько копий в разных местах
- Используйте облачное хранилище (Google Drive, Dropbox, OneDrive)
- Названия файлов с датой: `database-backup-2025-01-15.db`

### Автоматизация (для VPS):

Создайте скрипт `backup.sh`:
```bash
#!/bin/bash
DATE=$(date +%Y-%m-%d-%H%M%S)
cp database.db backups/database-backup-$DATE.db
# Удалите старые копии (старше 30 дней)
find backups/ -name "database-backup-*.db" -mtime +30 -delete
```

Добавьте в cron (ежедневно в 3:00):
```bash
0 3 * * * /path/to/backup.sh
```

## 🚨 Что делать, если потеряли данные?

Если у вас нет резервной копии, данные восстановить **невозможно**. 

**Профилактика:**
- Настройте автоматическое резервное копирование
- Регулярно скачивайте копии вручную
- Храните копии в нескольких местах

## 💡 Альтернативные решения

### Использование внешней базы данных (PostgreSQL):

Для продакшена рекомендуется использовать PostgreSQL вместо SQLite:
- Данные хранятся на отдельном сервере
- Автоматические бэкапы
- Лучше для продакшена

Но это требует дополнительной настройки и может быть платным.

---

**Помните: резервное копирование - это ваша ответственность!** Регулярно делайте бэкапы!

