# 🚀 Инструкция по деплою

## Вариант 1: Render.com (Рекомендуется - БЕСПЛАТНО)

### Шаги:

1. **Создайте аккаунт** на [render.com](https://render.com) (можно через GitHub)

2. **Подключите репозиторий**:
   - Нажмите "New" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий с проектом

3. **Настройки сервиса**:
   - **Name**: `pickem-major-cs2` (или любое другое)
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: Free (бесплатный)

4. **Нажмите "Create Web Service"**

5. **Готово!** Render автоматически:
   - Установит зависимости
   - Создаст базу данных SQLite
   - Запустит сервер
   - Дам вам URL вида: `https://pickem-major-cs2.onrender.com`

### ⚠️ Важно:
- На бесплатном плане сервер "засыпает" после 15 минут бездействия
- Первый запрос после простоя может занять 30-60 секунд
- База данных сохраняется между перезапусками

---

## Вариант 2: Railway.app (БЕСПЛАТНО)

### Шаги:

1. **Создайте аккаунт** на [railway.app](https://railway.app)

2. **Подключите репозиторий**:
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Railway автоматически**:
   - Определит Node.js проект
   - Установит зависимости
   - Запустит сервер

4. **Готово!** Railway даст вам URL вида: `https://pickem-major-cs2.up.railway.app`

### ⚠️ Важно:
- Бесплатный план дает $5 кредитов в месяц
- База данных SQLite сохраняется в файловой системе

---

## Вариант 3: Vercel (Статический + Serverless)

### Шаги:

1. Установите Vercel CLI: `npm i -g vercel`
2. Войдите: `vercel login`
3. Деплой: `vercel`

**Примечание**: Vercel лучше подходит для статических сайтов. Для полноценного backend лучше использовать Render или Railway.

---

## Вариант 4: VPS (Vultr, DigitalOcean)

### Шаги:

1. **Создайте VPS** (Ubuntu 20.04+)

2. **Подключитесь по SSH**:
```bash
ssh root@your-server-ip
```

3. **Установите Node.js**:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

4. **Клонируйте репозиторий**:
```bash
git clone https://github.com/your-username/pickem-major-cs2.git
cd pickem-major-cs2
npm install
```

5. **Установите PM2**:
```bash
npm install -g pm2
pm2 start server.js
pm2 save
pm2 startup
```

6. **Настройте Nginx** (опционально):
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 🔧 Локальный запуск для тестирования

```bash
# Установите зависимости
npm install

# Запустите сервер
npm start

# Или для разработки с автоперезагрузкой
npm run dev
```

Сервер будет доступен на `http://localhost:3000`

---

## 📝 Проверка после деплоя

1. Откройте ваш URL (например, `https://pickem-major-cs2.onrender.com`)
2. Проверьте, что главная страница загружается
3. Попробуйте выбрать игрока и сделать прогноз
4. Откройте админ-панель (`/admin.html`)
5. Введите пароль `admin123`
6. Проставьте результаты для тестирования

---

## 🐛 Решение проблем

### Проблема: "Cannot find module"
**Решение**: Убедитесь, что `package.json` содержит все зависимости и выполните `npm install`

### Проблема: "Port already in use"
**Решение**: Измените порт в `server.js` или используйте переменную окружения `PORT`

### Проблема: База данных не создается
**Решение**: Проверьте права на запись в директорию проекта

### Проблема: Статические файлы не загружаются
**Решение**: Убедитесь, что папка `public` существует и содержит все файлы

---

## 💡 Рекомендации

- **Для продакшена**: Используйте Render.com или Railway.app (бесплатно, легко настроить)
- **Для разработки**: Используйте локальный сервер (`npm start`)
- **Для бэкапов**: Регулярно скачивайте файл `database.db` через SSH или через админ-панель (можно добавить экспорт)

---

## 📞 Поддержка

Если возникли проблемы с деплоем, проверьте:
1. Логи сервера в панели управления хостинга
2. Консоль браузера (F12) на наличие ошибок
3. Что все файлы загружены в репозиторий

