# 🌟 Деплой на Render.com (БЕСПЛАТНО)

## ✅ Проект готов для деплоя!

Проект полностью готов для деплоя на Render.com:
- ✅ `package.json` содержит все зависимости и скрипт `start`
- ✅ `server.js` использует `process.env.PORT` (Render установит автоматически)
- ✅ Статические файлы в папке `public/` настроены правильно
- ✅ База данных SQLite создастся автоматически при первом запуске

## 🚀 Пошаговая инструкция

### Шаг 1: Создайте аккаунт
1. Откройте https://render.com
2. Нажмите "Get Started for Free"
3. Войдите через GitHub (рекомендуется)

### Шаг 2: Подключите репозиторий
1. В Dashboard нажмите **"New"** → **"Web Service"**
2. Нажмите **"Connect account"** если еще не подключен GitHub
3. Выберите репозиторий: `topbloggirs-tech/major2025`
4. Нажмите **"Connect"**

### Шаг 3: Настройте сервис

**Основные настройки:**
- **Name**: `pickem-major-cs2` (или любое другое имя)
- **Region**: Выберите ближайший регион (например, Frankfurt)
- **Branch**: `master` (или `main` если у вас main)
- **Root Directory**: оставьте пустым (корень проекта)

**Build & Deploy:**
- **Environment**: `Node`
- **Build Command**: `npm install`
- **Start Command**: `npm start`

**Plan:**
- Выберите **"Free"** (бесплатный план)

### Шаг 4: Создайте сервис
1. Нажмите **"Create Web Service"**
2. Render начнет автоматический деплой

### Шаг 5: Дождитесь деплоя
- Render автоматически:
  - Установит зависимости (`npm install`)
  - Запустит сервер (`npm start`)
  - Создаст базу данных SQLite при первом запуске
  - Назначит URL вида: `https://pickem-major-cs2.onrender.com`

### Шаг 6: Готово! 🎉
Ваше приложение будет доступно по URL, который Render предоставит.

## ⚙️ Дополнительные настройки (опционально)

### Переменные окружения
Если нужно изменить админ-пароль через переменные окружения:
1. Settings → Environment
2. Add Environment Variable
3. Например: `ADMIN_PASSWORD=your-secure-password`

### Кастомный домен
1. Settings → Custom Domains
2. Add Custom Domain
3. Следуйте инструкциям для настройки DNS

### Автоматический деплой
По умолчанию включен:
- Автоматический деплой при push в `master`/`main`
- Можно отключить в Settings → Auto-Deploy

## ⚠️ Важные особенности бесплатного плана

### "Сон" сервера
- Сервер "засыпает" после **15 минут** бездействия
- Первый запрос после простоя может занять **30-60 секунд** (cold start)
- Это нормально для бесплатного плана!

### База данных
- SQLite база данных (`database.db`) сохраняется между перезапусками
- Данные не теряются при "пробуждении" сервера
- Для продакшена рекомендуется PostgreSQL (Render предоставляет бесплатный план)

### Лимиты
- Неограниченное количество деплоев
- 750 часов работы в месяц (достаточно для постоянной работы)
- 100 GB исходящего трафика в месяц

## 🔧 Проверка после деплоя

1. **Откройте ваш Render URL** (например, `https://pickem-major-cs2.onrender.com`)
2. **Проверьте главную страницу** - должна загрузиться
3. **Выберите игрока** и сделайте прогноз
4. **Откройте админ-панель**: `https://your-app.onrender.com/admin.html`
5. **Введите пароль**: `admin123` (по умолчанию)
6. **Проставьте результаты** для тестирования

## 🐛 Решение проблем

### Проблема: "Build failed"
**Решение:**
- Проверьте логи в Render Dashboard → Logs
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что `package.json` содержит скрипт `start`

### Проблема: "Application error"
**Решение:**
- Проверьте логи в Render Dashboard → Logs
- Убедитесь, что порт используется из `process.env.PORT`
- Проверьте, что папка `public/` существует и содержит файлы

### Проблема: Статические файлы не загружаются
**Решение:**
- Убедитесь, что папка `public/` существует
- Проверьте путь в `server.js`: `app.use(express.static('public'))`
- Убедитесь, что файлы загружены в репозиторий

### Проблема: База данных не создается
**Решение:**
- Проверьте логи - база создается при первом запуске
- Убедитесь, что у приложения есть права на запись
- Проверьте, что `database.db` не в `.gitignore` (он должен быть там, но создастся на сервере)

### Проблема: Долгая загрузка после простоя
**Решение:**
- Это нормально для бесплатного плана (cold start)
- Первый запрос после 15 минут простоя займет 30-60 секунд
- Последующие запросы будут быстрыми

## 📊 Мониторинг

### Логи
- Render показывает логи в реальном времени
- Dashboard → Logs → View Logs
- Можно фильтровать по уровню (Info, Warning, Error)

### Метрики
- Dashboard показывает использование ресурсов
- CPU, Memory, Network
- Доступно в разделе Metrics

## 🔄 Обновление приложения

### Автоматическое обновление
- По умолчанию включено
- При push в `master`/`main` → автоматический деплой

### Ручной деплой
1. Dashboard → Manual Deploy
2. Выберите коммит
3. Нажмите "Deploy"

### Откат к предыдущей версии
1. Dashboard → Deploys
2. Найдите нужный деплой
3. Нажмите "..." → "Redeploy"

## 💡 Рекомендации

1. **Для продакшена:**
   - Используйте PostgreSQL вместо SQLite (Render предоставляет бесплатный план)
   - Настройте кастомный домен
   - Регулярно делайте бэкапы базы данных

2. **Для разработки:**
   - Используйте локальный сервер (`npm start`)
   - Тестируйте изменения локально перед деплоем

3. **Бэкапы:**
   - Регулярно скачивайте `database.db` через админ-панель
   - Или используйте PostgreSQL с автоматическими бэкапами

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Render Dashboard
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все файлы загружены в репозиторий
4. Проверьте документацию Render: https://render.com/docs

---

## ✅ Чеклист перед деплоем

- [ ] Код загружен в GitHub репозиторий `topbloggirs-tech/major2025`
- [ ] Все файлы в репозитории (кроме `node_modules/` и `database.db`)
- [ ] `package.json` содержит все зависимости
- [ ] `server.js` использует `process.env.PORT`
- [ ] Папка `public/` содержит все статические файлы
- [ ] Создан аккаунт на Render.com
- [ ] Репозиторий подключен к Render
- [ ] Настроен Web Service с правильными параметрами

**Готово к деплою!** 🚀


