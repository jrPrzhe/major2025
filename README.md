# Pick'Em Major CS2 — Ноябрь 2025

Веб-приложение для проведения дружеского Pick'Em-турнира по CS2 Major.

## 🚀 Быстрый старт

### Локальная разработка

1. Установите зависимости:
```bash
npm install
```

2. Запустите сервер:
```bash
npm start
```

3. Откройте в браузере:
```
http://localhost:3000
```

### Для разработки с автоперезагрузкой:
```bash
npm run dev
```

## 📦 Структура проекта

```
major2025/
├── server.js          # Backend сервер (Node.js + Express)
├── package.json       # Зависимости проекта
├── database.db        # SQLite база данных (создается автоматически)
├── public/            # Статические файлы (фронтенд)
│   ├── index.html     # Главная страница
│   ├── admin.html     # Админ-панель
│   ├── css/           # Стили
│   └── js/            # JavaScript файлы
│       ├── api.js     # API клиент
│       ├── app-api.js # Основная логика (работает с API)
│       ├── admin.js   # Логика админ-панели
│       └── timer.js   # Таймеры
└── README.md          # Документация
```

## 🗄️ База данных

Используется SQLite (файл `database.db`). Таблицы создаются автоматически при первом запуске:

- **players** - игроки (id, name, score)
- **picks** - прогнозы игроков (player_id, stage_id, slot_key, team_name)
- **admin_results** - результаты турнира от админа (stage_id, category, slot_index, team_name)

## 🌐 API Endpoints

### Игроки
- `GET /api/players` - получить всех игроков
- `GET /api/players/:id` - получить игрока по ID
- `PUT /api/players/:id` - обновить игрока
- `GET /api/players/:id/picks` - получить прогнозы игрока
- `POST /api/players/:id/picks` - сохранить прогноз
- `DELETE /api/players/:id/picks` - удалить прогноз

### Админ
- `GET /api/admin/results/:stageId` - получить результаты стадии
- `POST /api/admin/results/:stageId` - сохранить результаты стадии

## 🚢 Деплой

### Вариант 1: Render.com (Рекомендуется - бесплатно)

1. Создайте аккаунт на [Render.com](https://render.com)
2. Подключите GitHub репозиторий
3. Создайте новый Web Service:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Environment**: Node
4. Render автоматически определит порт из переменной `PORT`

### Вариант 2: Railway.app (Бесплатно)

1. Создайте аккаунт на [Railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Railway автоматически определит Node.js проект
4. Добавьте переменную окружения `PORT` (Railway установит автоматически)

### Вариант 3: Heroku (Платно, но есть бесплатный план)

1. Установите Heroku CLI
2. Войдите: `heroku login`
3. Создайте приложение: `heroku create pickem-major-cs2`
4. Деплой: `git push heroku main`

### Вариант 4: VPS (Vultr, DigitalOcean и т.д.)

1. Установите Node.js на сервере
2. Клонируйте репозиторий
3. Установите зависимости: `npm install`
4. Используйте PM2 для запуска: `pm2 start server.js`
5. Настройте Nginx как reverse proxy

## 🔧 Настройка

### Переменные окружения

- `PORT` - порт сервера (по умолчанию 3000)

### Админ-панель

Пароль администратора по умолчанию: `admin123`

Изменить можно в файле `public/js/admin.js`:
```javascript
const ADMIN_PASSWORD = 'ваш_пароль';
```

## 📝 Особенности

- ✅ Полностью автономное приложение
- ✅ SQLite база данных (не требует отдельного сервера БД)
- ✅ RESTful API
- ✅ Админ-панель для управления результатами
- ✅ Автоматический расчет очков
- ✅ Таймеры для каждой стадии
- ✅ Адаптивный дизайн

## 🛠️ Технологии

- **Backend**: Node.js, Express, SQLite3
- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Database**: SQLite

## 📄 Лицензия

MIT

