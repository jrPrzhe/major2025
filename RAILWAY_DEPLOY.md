# 🚂 Деплой на Railway

## ⚠️ ВАЖНО: Ограничения бесплатного плана Railway

**Railway изменил политику бесплатного плана!** Теперь бесплатный аккаунт может деплоить **только базы данных**, а не веб-приложения.

### Решения:

1. **Обновить план Railway** (платно) - от $5/месяц
2. **Использовать Render.com** (БЕСПЛАТНО) - лучшая альтернатива! ⭐
3. **Использовать Fly.io** (БЕСПЛАТНО) - еще один вариант

## 🎯 Рекомендация: Используйте Render.com

Render.com предлагает **полностью бесплатный** план для веб-приложений:
- ✅ Бесплатный хостинг Node.js приложений
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатный SSL сертификат
- ✅ Постоянное хранение данных

**См. инструкцию ниже или файл `DEPLOY.md` для Render.com**

---

## ✅ Проверка готовности проекта

Проект полностью готов для деплоя (Railway, Render, Fly.io):

- ✅ `Procfile` настроен правильно (`web: node server.js`)
- ✅ `package.json` содержит все зависимости и скрипт `start`
- ✅ `server.js` использует `process.env.PORT` (Railway автоматически установит)
- ✅ Статические файлы в папке `public/`
- ✅ База данных SQLite создастся автоматически при первом запуске

## 📤 Загрузка кода в новый репозиторий

### Вариант 1: Через GitHub UI (Рекомендуется)

1. Откройте https://github.com/topbloggirs-tech/major2025
2. Нажмите "Uploading an existing file" или используйте GitHub Desktop
3. Загрузите все файлы проекта (кроме `node_modules/` и `database.db`)

### Вариант 2: Через командную строку (если есть доступ)

```bash
# Если у вас есть доступ к репозиторию topbloggirs-tech
git remote set-url origin https://github.com/topbloggirs-tech/major2025.git
git push -u origin master
```

### Вариант 3: Создать новый репозиторий и скопировать файлы

1. Создайте новый репозиторий на GitHub
2. Скопируйте все файлы через GitHub UI или используйте GitHub Desktop

## 🚀 Деплой на Railway (требует платный план)

> ⚠️ **Внимание**: Бесплатный план Railway больше не поддерживает деплой веб-приложений. Нужен платный план от $5/месяц.

### Шаги (если у вас платный план):

1. **Войдите на Railway.app**
   - Откройте https://railway.app
   - Войдите через GitHub
   - **Обновите план** (Settings → Billing → Upgrade)

2. **Создайте новый проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите репозиторий `topbloggirs-tech/major2025`

3. **Railway автоматически:**
   - Определит Node.js проект
   - Установит зависимости (`npm install`)
   - Запустит сервер через `Procfile`
   - Назначит порт через переменную окружения `PORT`

4. **Настройте домен (опционально)**
   - В настройках проекта → Settings → Generate Domain
   - Railway даст вам URL вида: `https://your-app-name.up.railway.app`

5. **Готово!** 🎉

---

## 🌟 Альтернатива: Деплой на Render.com (БЕСПЛАТНО)

### Шаги:

1. **Создайте аккаунт** на [render.com](https://render.com) (можно через GitHub)

2. **Подключите репозиторий**:
   - Нажмите "New" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий `topbloggirs-tech/major2025`

3. **Настройки сервиса**:
   - **Name**: `pickem-major-cs2` (или любое другое)
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: Free (бесплатный)

4. **Нажмите "Create Web Service"**

5. **Готово!** Render автоматически:
   - Установит зависимости
   - Создаст базу данных SQLite
   - Запустит сервер
   - Дам вам URL вида: `https://pickem-major-cs2.onrender.com`

### ⚠️ Важно про Render:
- На бесплатном плане сервер "засыпает" после 15 минут бездействия
- Первый запрос после простоя может занять 30-60 секунд
- База данных сохраняется между перезапусками
- **Полностью бесплатно!** ✅

---

## 🚁 Альтернатива: Деплой на Fly.io (БЕСПЛАТНО)

### Шаги:

1. **Установите Fly CLI**:
   ```bash
   # Windows (PowerShell)
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **Войдите в Fly.io**:
   ```bash
   fly auth login
   ```

3. **Создайте приложение**:
   ```bash
   fly launch
   ```
   - Выберите регион
   - Назовите приложение

4. **Деплой**:
   ```bash
   fly deploy
   ```

5. **Готово!** Получите URL вида: `https://your-app.fly.dev`

### ⚠️ Важно про Fly.io:
- Бесплатный план: 3 shared-cpu-1x VMs
- Достаточно для небольшого приложения
- База данных SQLite сохраняется

## ⚙️ Дополнительные настройки Railway

### Переменные окружения (если нужны)

В Railway можно добавить переменные окружения:
- Settings → Variables → New Variable
- Например, для админ-пароля можно добавить `ADMIN_PASSWORD=your-password`

### Мониторинг

- Railway автоматически показывает логи в реальном времени
- В разделе "Deployments" можно видеть историю деплоев
- В разделе "Metrics" можно видеть использование ресурсов

## 🔧 Проверка после деплоя

1. Откройте ваш Railway URL
2. Проверьте главную страницу
3. Выберите игрока и сделайте прогноз
4. Откройте админ-панель (`/admin.html`)
5. Введите пароль (по умолчанию `admin123`)

## ⚠️ Важные замечания

1. **База данных SQLite:**
   - Создастся автоматически при первом запуске
   - Сохраняется между перезапусками
   - Для продакшена рекомендуется использовать PostgreSQL (Railway предоставляет бесплатный план)

2. **Платный план Railway:**
   - От $5/месяц (Hobby план)
   - $5 кредитов в месяц включено
   - Автоматическое масштабирование
   - ⚠️ Бесплатный план больше не поддерживает веб-приложения

3. **Резервное копирование:**
   - Регулярно скачивайте `database.db` через админ-панель
   - Или используйте Railway Volume для постоянного хранения

## 🐛 Решение проблем

### Проблема: "Build failed"
- Проверьте, что все зависимости указаны в `package.json`
- Убедитесь, что `Procfile` существует и правильный

### Проблема: "Application error"
- Проверьте логи в Railway Dashboard
- Убедитесь, что порт используется из `process.env.PORT`

### Проблема: Статические файлы не загружаются
- Убедитесь, что папка `public/` существует
- Проверьте путь в `server.js`: `app.use(express.static('public'))`

## 📝 Следующие шаги

После успешного деплоя:
1. Настройте кастомный домен (если нужно)
2. Настройте автоматические бэкапы базы данных
3. Добавьте мониторинг (опционально)
4. Настройте CI/CD для автоматического деплоя при push в master

