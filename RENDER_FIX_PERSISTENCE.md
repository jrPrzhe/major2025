# 🔧 Решение проблемы сохранения данных на Render.com

## Проблема

На Render.com база данных SQLite создается во временной директории, которая очищается при перезапуске сервера. Данные теряются.

## ✅ Решение 1: Использовать Render Disk (Рекомендуется - простое)

Render Disk - это постоянное хранилище, которое сохраняется между перезапусками.

### Шаги:

1. **В Render Dashboard:**
   - Откройте ваш Web Service
   - Перейдите в **Settings** → **Disks**
   - Нажмите **"Create Disk"**
   - **Name**: `database-storage`
   - **Size**: 1 GB (достаточно для SQLite)
   - **Mount Path**: `/data`
   - Нажмите **"Create Disk"**

2. **Обновите server.js** (уже сделано ниже):
   - Измените путь к базе данных на `/data/database.db`
   - Это сохранит базу данных на постоянном диске

3. **Перезапустите сервис:**
   - Render автоматически перезапустит приложение
   - База данных теперь будет сохраняться!

### Обновление кода:

Нужно изменить путь к базе данных в `server.js`:

```javascript
// Было:
const dbPath = path.join(__dirname, 'database.db');

// Стало:
const dbPath = process.env.DATABASE_PATH || path.join(__dirname, 'database.db');
```

И добавить переменную окружения в Render:
- Settings → Environment → Add Environment Variable
- **Key**: `DATABASE_PATH`
- **Value**: `/data/database.db`

---

## ✅ Решение 2: Использовать PostgreSQL (Более надежно)

PostgreSQL - это полноценная база данных, которая гарантированно сохраняет данные.

### Шаги:

1. **Создайте PostgreSQL базу данных в Render:**
   - В Render Dashboard нажмите **"New"** → **"PostgreSQL"**
   - **Name**: `pickem-db` (или любое другое)
   - **Database**: `pickem` (или любое другое)
   - **User**: будет создан автоматически
   - **Plan**: Free (бесплатный)
   - Нажмите **"Create Database"**

2. **Подключите базу данных к Web Service:**
   - Откройте ваш Web Service
   - Settings → Environment
   - Render автоматически добавит переменную `DATABASE_URL`
   - Скопируйте значение `DATABASE_URL`

3. **Обновите код для поддержки PostgreSQL:**
   - Нужно установить `pg` пакет
   - Обновить `server.js` для работы с PostgreSQL
   - (См. файл `server-postgres.js` ниже)

---

## 🚀 Быстрое решение: Render Disk

Это самое простое решение - не требует изменения кода, только настройку в Render.

### Что нужно сделать:

1. **Создайте Disk в Render:**
   ```
   Settings → Disks → Create Disk
   Name: database-storage
   Size: 1 GB
   Mount Path: /data
   ```

2. **Добавьте переменную окружения:**
   ```
   Settings → Environment → Add Variable
   Key: DATABASE_PATH
   Value: /data/database.db
   ```

3. **Обновите server.js** (я сделаю это за вас)

4. **Перезапустите сервис**

Готово! Данные теперь сохраняются между перезапусками.

---

## 📝 Проверка

После настройки:

1. Создайте тестовые данные (выберите игрока, сделайте прогноз)
2. Подождите 15+ минут (сервер "заснет")
3. Откройте приложение снова (сервер "проснется")
4. Проверьте, что данные сохранились

---

## ⚠️ Важно

- **Render Disk** - бесплатно до 1 GB
- **PostgreSQL** - бесплатно до 90 дней, потом нужно платить или мигрировать
- Для долгосрочного использования лучше PostgreSQL, но для начала Render Disk достаточно

---

## 🔄 Миграция данных (если уже есть данные)

Если у вас уже есть данные в старой базе:

1. Скачайте текущую базу через админ-панель (`/api/admin/backup`)
2. Настройте Render Disk
3. Загрузите базу данных на новый диск (через SSH или через админ-панель после обновления)

---

## 💡 Рекомендации

1. **Для начала**: Используйте Render Disk (проще, бесплатно)
2. **Для продакшена**: Используйте PostgreSQL (надежнее, но требует обновления кода)
3. **Регулярные бэкапы**: Используйте `/api/admin/backup` для скачивания базы данных


